Please completely refactor our current repository to build a comprehensive, side-by-side Markdown README Editor with Live Preview. 

1. UI Layout: Divide the screen into two equal columns. The left panel must be a raw text area where a developer can type markdown. The right panel must display a real-time, stylized "Live Preview" of how the text looks rendered on GitHub.
2. Template Sidebar: Include a sidebar panel with clickable templates like "Project Title", "Installation Steps", "Usage", and "License" that insert standard boilerplate Markdown snippets into the editor at the cursor position.
3. Live Rendering: Use JavaScript to instantly parse the text area markdown into clean HTML markup on the right panel as the user types.
4. Export: Add an action button at the top that lets users instantly download the text as a formatted 'README.md' file.
5.
